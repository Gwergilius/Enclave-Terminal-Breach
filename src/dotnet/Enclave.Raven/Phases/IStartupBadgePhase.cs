using Enclave.Shared.Phases;

namespace Enclave.Raven.Phases;

/// <summary>
/// Runs the startup badge: product name, version, and load time (RAVEN-Requirements §1).
/// </summary>
public interface IStartupBadgePhase : IPhase
{
}
