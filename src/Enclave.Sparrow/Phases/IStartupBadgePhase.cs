namespace Enclave.Sparrow.Phases;

/// <summary>
/// Runs the startup badge: product name, version, and load time (SPARROW-Requirements §1).
/// </summary>
public interface IStartupBadgePhase : IPhase
{
}
